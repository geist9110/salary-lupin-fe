version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - export NODE_ENV=development
      - npm ci
  build:
    commands:
      - npm run build

artifacts:
  base-directory: dist
  files:
    - '**/*'